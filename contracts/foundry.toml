[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200

[rpc_endpoints]
moonbase = "${MOONBASE_RPC_URL}"

[etherscan]
moonbase = { key = "${MOONSCAN_API_KEY}", url = "https://api-moonbase.moonscan.io/api" }
